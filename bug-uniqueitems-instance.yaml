apiVersion: kro.run/v1alpha1
kind: UniqueTest
metadata:
  name: test-duplicates
spec:
  # Try to add duplicate tags - should be rejected if uniqueItems works
  tags:
    - "tag1"
    - "tag2"
    - "tag1"  # Duplicate!
  name: test
